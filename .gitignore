# Build outputs
**/bin/
**/obj/
publish/

# IDE and local tooling
.vs/
.vscode/
.idea/
.trae/

# Local application data
data/*
!data/.gitkeep

# Local configuration and secrets
appsettings.Local.json
appsettings.*.Local.json
*.user
*.suo

# OS files
.DS_Store
Thumbs.db

# Local model training (datasets, environments, checkpoints, and generated reports)
training/.venv/
training/cache/
training/data/raw/
training/data/processed/
training/data/strict/
training/data/strict-diverse/
training/artifacts/
training/reports/generated/
__pycache__/
*.py[cod]

# Legacy local corpus. The versioned root-level `知识库/` directory is intentionally
# shipped with releases and therefore must not be added to this ignore list.
明日方舟设定/

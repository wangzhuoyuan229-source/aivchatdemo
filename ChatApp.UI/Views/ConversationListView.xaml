<UserControl x:Class="ChatApp.UI.Views.ConversationListView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ChatApp.UI.ViewModels"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,14,12,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="会话" Classes="title"/>
            <Button Grid.Column="1" Classes="ghost" Content="↻" Command="{Binding RefreshCommand}"/>
        </Grid>

        <Grid Grid.Row="1" Margin="14,0,14,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchText}"
                     KeyDown="SearchBox_KeyDown"/>
            <Button Grid.Column="1" Classes="ghost" Content="搜索" Command="{Binding SearchCommand}"/>
        </Grid>

        <ListBox Grid.Row="2" ItemsSource="{Binding Items}" Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" DoubleTapped="Conversations_DoubleTapped">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="14,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="40" Height="40" CornerRadius="6" Background="#F2F2F2" VerticalAlignment="Top">
                            <TextBlock Text="{Binding Avatar}" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="12,2,8,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="13" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding RoleName}" FontSize="11" Foreground="{StaticResource TextSubBrush}" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding UpdatedText}" FontSize="10" Foreground="#AAA" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="2" Classes="ghost" Content="✕"
                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}" VerticalAlignment="Center"
                                ToolTip.Tip="删除会话"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>

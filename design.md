# ChatApp UI 设计规范

本文档是 ChatApp 的统一视觉基线。新建或迭代页面前必须先阅读本文档，并优先复用 `ChatApp.UI/Themes/Light.xaml`、`Dark.xaml` 与 `App.xaml` 中已有的语义化资源，禁止在页面内随意新增近似色、近似字号或无依据的圆角。

## 1. 设计方向

整体采用 Apple 桌面应用式的克制、清晰与直接反馈：导航、列表、内容通过系统灰阶和轻材质形成层级；Apple 系统蓝只用于主操作、焦点与状态强调；大面积区域保持安静，卡片只用于需要独立分组的信息。界面优先保证可预测性、信息扫描效率、呼吸感和长期使用舒适度。

设计原则遵循 Purpose、Agency、Familiarity、Simplicity 与 Craft：每个元素必须有明确用途；高频操作不增加阻塞性动画；按压立即反馈；弹层与触发位置保持空间关系；深浅主题都使用平台化语义资源，不在页面内硬编码近似颜色。

### 布局结构

- 主窗口默认 `1280 × 820`，最小 `960 × 600`。
- 一级导航轨宽 `76px`，使用低对比灰色背景。
- 会话/角色列表宽 `350px`，与内容区之间用 `1px` 分隔线。
- 详情内容区自适应剩余宽度；页面内容与窗口边缘通常保持 `24–28px`。
- 聊天页采用“顶部会话栏 / 消息流 / 底部输入区”三段式结构；工具动作靠右排列。
- 设置、知识库等管理页使用清晰的页标题和说明，再进入分组内容；不要用装饰性大标题挤占空间。

## 2. 配色

### 浅色主题（主基准）

| 语义 | 资源名 | 色值 | 用途 |
| --- | --- | --- | --- |
| 主色 | `AccentBrush` | `#007AFF` | 主按钮、焦点、链接与关键状态 |
| 主色按下/悬停 | `AccentDarkBrush` | `#0068D8` | 主按钮悬停与强调文字 |
| 主色浅底 | `AccentSoftBrush` | `#E7F2FF` | 导航选中、状态标签、引用标签 |
| 页面背景 | `BgBrush` | `#F2F2F7` | 聊天区与管理页 grouped background |
| 一级导航背景 | `NavRailBrush` | `#ECECF1` | 最左侧导航轨 |
| 列表面板背景 | `ListPaneBrush` | `#F7F7FA` | 角色、会话与知识分组区域 |
| 面板/卡片背景 | `PanelBrush` | `#FCFCFE` | 表单、卡片、输入框 |
| 浮动栏材质 | `ChromeBrush` | `#F2FCFCFE` | 顶栏、底部输入栏等轻材质区域 |
| 次级浅底 | `SubtleBrush` | `#ECECF1` | 默认头像、弱按钮、占位区域 |
| 中性选中 | `SelectionBrush` | `#E5E5EA` | 角色列表选中；禁止使用蓝色整块背景 |
| 主文字 | `TextMainBrush` | `#1C1C1E` | 标题与正文 |
| 次文字 | `TextSubBrush` | `#636366` | 说明、元数据、次级操作 |
| 禁用文字 | `TextDisabledBrush` | `#AEAEB2` | 禁用、空状态与弱提示 |
| 分隔线 | `DividerBrush` | `#D8D8DC` | 面板边界和结构分隔 |
| 控件边框 | `ControlBorderBrush` | `#D1D1D6` | 输入框、下拉框、次按钮 |
| 卡片边框 | `CardBorderBrush` | `#14000000` | 设置卡片、文档卡片 |
| 用户气泡 | `UserBubbleBrush` | `#D9ECFF` | 当前用户消息 |
| 对方气泡 | `AssistantBubbleBrush` | `#FFFFFF` | AI/其他角色消息 |
| 危险色 | `DangerBrush` | `#FF3B30` | 删除与不可逆操作 |

### 深色主题

深色主题沿用相同语义资源，不在页面内判断主题。核心值为：页面 `#000000`、面板 `#1C1C1E`、列表 `#161618`、主色 `#0A84FF`、主文字 `#F2F2F7`、次文字 `#98989D`、分隔线 `#38383A`。新增组件必须使用 `DynamicResource`，确保主题可以实时切换。

### 使用规则

- 单页蓝色强调面积原则上不超过可视区域的 12%；不要把大面积背景涂成主色。
- 普通正文禁止使用纯黑 `#000000`；主文字统一使用 `TextMainBrush`。
- 层级优先通过背景灰阶与留白表达，避免增加粗边框。
- 危险色只用于删除、清空等高风险动作；警告使用现有 `WarnBrush`。

## 3. 字体

字体栈为 `SF Pro Text, PingFang SC, Inter, Microsoft YaHei UI, sans-serif`。macOS 优先系统 SF 字体，中文使用苹方；其他平台回退到 Inter 与系统中文字体。

| 层级 | 字号 | 字重 | 行高 | 用途 |
| --- | ---: | --- | ---: | --- |
| 页面标题 | `24px` | `SemiBold` / 600 | `31px` | 设置、独立管理页标题 |
| 栏标题 | `17–19px` | `SemiBold` / 600 | `24–26px` | 会话标题、列表标题、知识库标题 |
| 卡片标题 | `14px` | `SemiBold` / 600 | `21px` | 设置分组、文档标题 |
| 正文 | `13–14px` | `Regular` / 400 | `20–22px` | 表单、按钮、聊天正文 |
| 辅助文字 | `12px` | `Regular` / 400 | `18px` | 标签、说明、摘要 |
| 元数据 | `10–11px` | `Regular` / 400 | `16px` | 时间、计数、来源 |

正文与背景必须保持明确对比；不要依赖字重代替字号层级，也不要在同一模块中使用超过三种字号。

## 4. 间距与留白

统一使用 `4px` 基础网格。

| 令牌 | 数值 | 常见用途 |
| --- | ---: | --- |
| `space-1` | `4px` | 图标与短标签、紧凑元素 |
| `space-2` | `8px` | 同组按钮、标签、字段内间距 |
| `space-3` | `12px` | 头像与文字、气泡内部、列表项纵向间距 |
| `space-4` | `16px` | 普通模块间距、列表内边距 |
| `space-5` | `20px` | 卡片内边距、页头与内容 |
| `space-6` | `24px` | 页面边距、设置卡片内边距 |
| `space-7` | `28px` | 宽屏管理页外边距、聊天消息水平边距 |
| `space-8` | `32px` | 大模块分隔，谨慎使用 |

规则：同一组元素间距小于不同模块间距；页面边距不得小于 `20px`；聊天消息上下间距为 `20px`（单项 `10px` 外边距）；密集表单字段通常相隔 `12px`。

## 5. 组件样式

### 按钮

- 主按钮：系统蓝实底、白字、`10px` 圆角、无边框，水平内边距 `20–24px`，高度至少 `38px`。
- 次按钮：次级浅底、`1px ControlBorderBrush`、`10px` 圆角；悬停使用 `HoverOverlayBrush`。
- 幽灵按钮：透明底、无边框、`10px` 圆角，用于工具栏和行内低优先级操作。
- 图标按钮：常用 `40–44px` 方形点击区，图标本身约 `18–20px`；不要只提供小于 `32px` 的点击目标。
- 同一区域只能有一个视觉主按钮；删除操作不使用蓝色主按钮。
- 所有按钮在按下瞬间降低至约 `72%` 不透明度，反馈必须从 pointer-down 开始；高频导航不添加阻塞式进出动画。

### 输入框与下拉框

- 高度至少 `38px`，聊天输入框至少 `52px`。
- 背景使用 `PanelBrush`，边框 `1px ControlBorderBrush`，圆角 `10px`；聊天输入容器使用 `16px`。
- 普通内边距 `11px 8px`；多行聊天输入使用 `14px 10px`。
- 标签置于字段上方，使用 `12px TextSubBrush`，标签与控件间隔 `4px`。

### 卡片

- 仅用于设置分组、知识文档、弹层等需要独立边界的模块；普通聊天列表不使用悬浮卡片。
- 圆角 `16px`，边框 `1px CardBorderBrush`，内部留白 `20–24px`。
- 默认阴影 `0 1px 4px #10000000`；浮层阴影 `0 12px 36px #22000000`。
- 相邻卡片间距 `18px`。不要叠加粗边框、重阴影和高饱和背景。

### 导航栏与列表

- 一级导航轨宽 `76px`；导航按钮 `44 × 44px`、圆角 `12px`，垂直间距约 `10px`（按钮外边距各 `5px`）。
- 一级导航选中态使用 `AccentSoftBrush`；角色列表选中态使用中性 `SelectionBrush`，不得使用蓝色整块背景。
- 二级列表面板宽 `350px`；列表项圆角 `8–10px`，内边距约 `10–12px`。
- 列表标题与摘要使用主/次文字色区分，时间与计数使用 `10–11px` 元数据样式。

### 消息气泡

- 头像 `40 × 40px`、圆角 `9px`，头像与气泡间隔 `10px`。
- 气泡最大宽度 `580px`、圆角 `16px`、内边距 `15px 11px`。
- 用户消息使用浅蓝色，对方消息使用面板白色；仅使用 `0 1px 2px` 的极轻阴影。
- 消息流水平留白 `28px`，单条消息上下外边距 `10px`。

### 弹层与对话框

- 遮罩使用约 40% 黑色；容器圆角 `16px`、内边距 `24px`、使用浮层阴影。
- 标题在上、正文居中区域、动作按钮在右下；动作之间间隔 `8px`。
- 危险确认必须明确标红危险动作，默认焦点优先放在安全操作上。

## 6. 实现检查清单

提交 UI 改动前检查：

1. 是否复用了现有 `DynamicResource`，且浅色/深色主题都可读。
2. 是否遵循 `4px` 网格、标题层级和页面最小留白。
3. 是否只有一个主操作，危险动作是否正确使用危险色。
4. 是否避免了无意义卡片、过重阴影、粗边框和大面积高饱和蓝色。
5. XAML 绑定名、命令和 ViewModel 成员是否一致，并完成解决方案构建。
